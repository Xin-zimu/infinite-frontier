# V0.11.0 Visual Evidence

`basic-enemies.svg` is the editable source and `basic-enemies.png` is the inspected 1280×720 release image. It reconstructs the runtime's exact V0.11 population limits, distance gates, biome assignments, state names and drop IDs while displaying the three procedural enemy silhouettes.
