# V0.3.0 Screenshots

- `deterministic-chunk.png` is an actual 1280×720 OpenGL frame of the four-class terrain view after isolated-cell cleanup.
- `noise-debug-view.png` is an actual frame of the same seed, signed chunk coordinate and checksum rendered through the 16-step elevation debug palette.

Both frames were captured from Godot 4.7.1 with the compatibility renderer and inspected for Chinese glyph coverage, HUD layout and tile-layer correctness.
