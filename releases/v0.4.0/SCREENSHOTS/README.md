# V0.4.0 Screenshots

`infinite-chunk-stream.png` is an actual 1280×720 OpenGL frame captured after stream warm-up. It shows a view spanning signed chunk boundaries with 25 active renderers, 24 preloaded data chunks, 49 cached chunks, zero queued jobs and zero active workers. Gold lines are the runtime boundary overlay toggled by `B`.
