# 无尽边境 · Infinite Frontier

一款完全本地运行的 2D 俯视角程序化无限世界生存探索 RPG。项目按照可回退、可测试、可独立运行的版本路线，从 V0.1.0 连续迭代至最终 V5.0.0。

> 当前版本：**V4.0.0 - 完整生存建造版**

## 核心原则

| 原则 | 说明 |
|---|---|
| 完全离线 | 游戏不连接 API，不依赖云服务或本地大模型 |
| 确定性世界 | 相同种子与坐标永远生成相同基础世界 |
| 无限探索 | 以区块为单位动态生成、加载、休眠与卸载 |
| 差异存档 | 仅保存玩家造成的永久变化 |
| 逐版验收 | 每一版本都具有测试、审查、构建和 Git Tag |

Stable tags are created by the repository release gate only when the matching test report is marked `PASS`; existing tags are never moved.

## V4.0.0 新增

- 按 `X` 打开家园与基地界面；在 `K` 建造界面使用 8 木材、8 石材和 2 铁锭放置家园信标，即可建立长期基地
- 每个世界最多管理 3 座基地，信标间距至少 32 格；每座基地以信标为唯一身份，在 24 格半径内统计建筑、储物、农田、动物、加工与自动化资产
- 家园进度把房屋、储物、农业、养殖、烹饪药水、冶炼、装备强化和基础自动化汇总为清晰的八步完整生存建造循环
- 可在多座基地间切换当前家园，并从地表或地下安全返回信标；传送成功后才开始 120 秒游戏时间冷却，地牢内明确禁用
- 家园信标仍由原有区块建筑差分唯一拥有；玩家文档只保存基地名称、当前家园与传送时间，并在读写时与全部信标交叉校验
- 放置、拆除、农业、养殖、储物和机器变化都会立即刷新对应基地状态；拆除信标会同步移除基地，避免悬空引用
- 存档格式升级至 25；格式 24 迁移会从已有信标同步基地，但不会凭空生成信标、基地或传送冷却，世界生成格式继续保持 5

## V3.7.0 新增

- 按 `Y` 打开基础自动化界面；可查看全部机器的启停、工作状态、累计循环、自动熔炉配方、燃料与分拣过滤器
- 新增传送带、自动熔炉、储物连接器和物品分拣器 4 种可制作、放置和旋转的机器；物品目录扩展至 72 项，制作目录扩展至 28 个配方
- 储物箱与机器按朝向形成最长 12 格的物流线：传送带每 tick 搬运 1 件，储物连接器搬运 4 件，分拣器只放行选中的 2 件物品
- 自动熔炉从后方储物箱提取矿石与木材/煤块，按已选炼制配方消耗燃料，再把完整产物原子写入前方储物箱
- 机器每 5 秒结算一次，并保存启停、时间游标、状态、累计循环、燃料、配方和过滤器；区块卸载后仍按游戏时间有界补算
- 全局最多 128 台机器；单次推进最多执行 64 次有效操作、每台最多补算 720 tick，超限状态会明确显示为性能限流
- 所有物流先在完整建造快照上模拟，再通过严格 schema 原子提交；输出已满、输入不足或非法状态不会吞物或复制物品
- 存档格式升级至 24；格式 23 及更早存档保留既有建筑但不会凭空生成自动化机器或工作状态，世界生成格式继续保持 5

## V3.6.0 新增

- 按 `O` 打开装备界面；可把背包中的武器、防具和饰品登记为独立装备实例，并装备到武器、头盔、胸甲、战靴和两个饰品槽位
- 新增铜剑、铁剑、铜盔、铜甲、铜靴、远行护符和霜纹戒指；制作目录扩展至 24 个配方，物品目录扩展至 68 项
- 每件装备按稳定实例编号生成粗糙、标准、精良或大师品质，以及普通、优秀、稀有或史诗稀有度和对应数量的确定性词条
- 攻击、防御、生命和体力加成实时叠加到玩家运行时属性；铜卫三件套提供额外防御和生命套装奖励
- 淬火板材用于最高 +5 强化，铁锭用于修理；强化会提升属性，武器耐久降至零后仍保留且可修复，不会被静默删除
- 装备库、已穿戴槽位、强化等级、耐久、品质、稀有度和词条均由严格 schema 持久化；格式 22 迁移为空装备状态，不会凭空制造装备
- 存档格式升级至 23，世界生成格式保持 5，既有地形校验值不变

## V3.5.0 新增

- 按 `T` 打开烹饪与炼制界面；靠近已放置的烹饪锅或冶炼炉后，可投入木材/煤块并执行对应加工配方
- 新增烹饪锅、冶炼炉两种可制作和放置的加工设备；燃料容量、剩余燃料和累计加工次数随所属地表区块持久化
- 新增 10 个多材料配方，覆盖三种正餐、三种恢复药水、铜锭、铁锭、钢锭和淬火板材
- 田园炖菜、苹果派和丰盛早餐提供不同的饥饿/生命/体温恢复；解毒、暖身和清凉药剂可在零饥饿收益时按状态需要使用
- 木材提供 2 单位燃料、煤块提供 6 单位燃料；每次加工明确消耗配方燃料，容量不足、材料不足或背包已满都不会吞掉输入
- 加工设备成为建造系统中的独立热源，具有专用批量图块表现；仍有剩余燃料时拒绝拆除，避免静默丢失能量
- 存档格式 22 在既有区块所属建筑记录内保存 `fuel_units` 和 `processed_count`；格式 21 保留全部旧建筑并且不会凭空生成加工设备或燃料

## V3.4.0 新增

- 按 `U` 打开养殖界面；选择鸡、牛或羊后，在 6 格内对匹配的确定性野生动物按左键喂食、驯服或收取产物
- 鸡、牛、羊按世界种子、全局网格、群系和地形稳定生成；水域、资源与生成建筑不会出现动物，首次互动后转为持久化记录
- 每日喂食提升亲密度并提供两天饲料储备；达到物种阈值后完成驯服，蛋、牛奶和羊毛按游戏日产生且有明确库存上限
- 同种异性成年动物在距离、亲密度、冷却和围栏邻近条件全部满足时可繁殖；幼崽保持驯服并按物种天数成长
- 夜间已驯服动物进入睡眠并拒绝互动；昼夜与跨日事件会刷新活动区块，卸载区块仍从保存的游戏日做有界产出模拟
- 新增动物围栏蓝图，建造、农业和动物共享双向占位校验，不能在同一格重叠；活动区块使用独立批量动物渲染层
- 存档格式 21 仅在所属地表区块差分中保存已互动动物，`player.json` 不重复记录；格式 2–20 迁移为空养殖状态

## V3.3.0 新增

- 按 `H` 打开农业界面；在 6 格内对合法地表格按左键依次开垦、播种或收获，右键按当前状态执行浇水或施肥
- 新增小麦、胡萝卜、番茄和苹果树 4 类数据驱动作物，覆盖一年生、多次收获与果树再生
- 土壤、种子、浇水、基础肥料、分阶段生长、成熟与收获形成完整原子事务；失败不会吞掉种子、肥料或产物
- 晴、雨、雪和沙尘天气会分别影响浇水与生长倍率；跨日和休息使用有界离线区块模拟推进作物
- 每次收获由世界种子、地块稳定 ID、照料天数与收获次数确定普通、银星或金星品质，并产出对应稳定物品
- 耕地与玩家建筑共享占位校验，不能互相覆盖；活动区块使用独立批量农业渲染层显示开垦、湿润、生长和成熟状态
- 存档格式 20 仅在所属地表区块差分中保存农业地块，`player.json` 不重复记录；格式 2–19 迁移为空农业状态

## V3.2.0 新增

- 按 `K` 打开玩家建造界面；鼠标指向世界格实时显示合法/非法预览，`R` 以 90° 旋转，左键放置、右键按屋顶→结构→地面的顺序拆除
- 新增木地板、木墙、木门、基础屋顶、木桌、储物箱、落地火把、工作台和火堆 9 种数据驱动蓝图
- 放置会检查 6 格距离、玩家占位、水域、未采集资源、生成建筑/道路、分层占用、地板支撑和门的邻墙条件；失败不扣材料
- 地板、结构与屋顶使用共享图集分层批量渲染；墙体、关闭的门、家具、储物箱和工作台具有碰撞，开门会即时移除门碰撞
- 工作台和火堆在附近时接入既有制作配方；火堆和落地火把同时参与照明与生存热源判定
- 储物箱提供 8 格持久空间：选中快捷栏堆叠后按 `E` 存入，空选中格时按 `E` 取回；普通堆叠和耐久工具都保持完整元数据
- 拆除在背包有空间时原子返还 50% 配方材料（至少 1 个）；非空储物箱和仍支撑上层物件的地板不能拆除
- 存档格式 19 仅在所属地表区块差分中保存玩家建筑，`player.json` 不重复记录；拆空区块会清理旧差分文件，格式 2–18 迁移为空建造状态

## V3.1.0 新增

- 新增饥饿、体温与湿度三项持续生存属性；奔跑会额外消耗饥饿，浸水与降雨会累积湿度
- 群系、天气、昼夜、地表/洞穴/地牢、水域和附近热源共同决定体温与干燥速度，数值始终保持在验证边界内
- 新增中毒、燃烧、冻伤与饥饿四类数据驱动状态效果，包含持续时间、周期伤害、独立暴露阈值和移动惩罚
- 快捷栏选中浆果或熟浆果后按 `G` 食用；物品先原子扣除，再恢复饥饿、生命、体温或清除中毒
- 左上生存 HUD 实时显示三项属性、状态效果和规则开关；设置菜单可关闭整套生存衰减与环境伤害
- 死亡与旅店休息会执行明确的生存恢复规则，避免复活后立即再次受到残留状态伤害
- 存档格式 18 保存完整生存状态；格式 2–17 显式迁移为中性状态，不凭空添加伤害、湿度或状态效果

## V3.0.0 新增

- 固定任务扩展至 24 个完整模板：10 条连续主线与 14 条支线，覆盖采集、讨伐、探索和护送/会合目标
- 每个区域每天生成 3 条确定性随机委托；4 类规则、目标、数量、委托人和奖励均由世界种子稳定决定
- 随机委托定义与委托板有 96 条硬上限，旧的终态委托按整板清理，长期游玩不会让存档无限增长
- 新增 3 次关键世界选择，每个选项精确改变阵营声望并贡献一次去重世界进度，重复选择不会二次结算
- `L` 任务日志同时显示主线、支线、随机委托和可用世界选择，并提供清晰的类型与结果状态
- 存档格式 17 保存随机任务定义、委托板和世界选择；格式 16 的固定任务记录会无损迁移到任务状态 schema 2

## V2.6.0 新增

- 无限世界按 6×6 区块划分为稳定区域，距离带映射到 1–5 级危险度，并公开建议装备评分与敌人等级范围
- 装备评分取背包中每类最佳武器/工具，并叠加三枚区域 Boss 印记；当前上限正好覆盖最高危险建议值
- 每个敌人的等级与精英身份由世界种子、稳定出生 ID 和所在区域确定；等级缩放生命/攻击/防御，精英拥有金色轮廓与双倍掉落
- 首次发现区域、击败精英、完成任务/世界事件/地牢和击败区域 Boss 会产生去重世界进度来源
- 林冠巨像默认开放；沙海巨兽与霜穹翼龙分别在 24/60 世界进度解锁，未解锁 Boss 不会进入运行时人口
- 每个区域都有一次性条件奖励；背包不足时整笔回滚，领取状态不会提前消耗
- `P` 打开区域进度界面，可查看危险度、装备评分、敌人范围、精英率、奖励条件和 Boss 门槛；右下角常驻摘要同步更新
- 存档格式 16 保存区域发现、去重来源和奖励领取；格式 2–15 显式迁移，V2.5 格式 15 不会凭空获得进度

## V2.5.0 新增

- 新增商队经过、敌袭村庄、流星坠落、资源爆发、暴风雪、遗迹开启、临时首领和救援事件八类数据驱动世界事件
- 世界种子决定事件轮换顺序；事件按游戏内时间表启停，并在激活时锚定玩家所在带符号区块
- 动态效果会与既有系统组合：改变敌人数量上限、资源掉落倍率，暴风雪会请求降雪天气
- 交易、击败敌人、采集、探索遗迹、击败区域首领和 NPC 会合会推进匹配事件目标；生存事件在持续时间结束时结算
- `V` 打开世界事件时间表，可查看当前事件、目标进度、六项预告和最近结果；常驻追踪器显示倒计时
- 历史最多保留 48 条，长时间休息或旧档迁移会有界快进，不按离线现实时间推进
- 存档格式 15 保存时间表游标、当前事件、目标进度和历史结果；V2.4 格式 14 自动迁移为空事件状态

## V2.4.0 新增

- 新增边境村盟、行商公会、远路探盟和灰烬掠团四个数据驱动阵营，以及完整的两两关系
- 阵营声望采用 `-100..100` 与死敌、敌视、中立、尊敬、同盟五级状态；交易、任务、探索和敌对击杀会实时改变对应声望
- 新增灰烬斥候地表敌人；击败灰烬成员会降低其阵营声望并提升边境村盟声望
- `F` 打开阵营档案，可查看关系、最近事件、已控制地点，以及按声望解锁和折扣结算的阵营物资
- 村庄发现会建立稳定控制点；敌对势力通过影响力争夺可改变归属，全部状态按稳定 ID 持久化
- 存档格式 14 保存四阵营声望、发现记录、事件日志与控制点；V2.3 格式 13 自动迁移为默认阵营状态

## V2.3.0 新增

- 内置 6 条连续主线与 4 条可重试支线，包含采集、讨伐、探索和护送/会合四类目标
- 任务前置条件、同时进行上限、完成待领取、奖励已领取、失败与重试均有明确状态
- 采集库存、地点发现、普通/精英/Boss 击败和 NPC 会合会自动推进匹配目标
- `L` 打开任务日志，可接取、追踪、放弃/重试和原子领取奖励；常驻 HUD 显示当前目标
- 奖励背包空间不足时完整回滚，任务仍保持待领取，不吞物品也不重复发放
- 存档格式 13 保存任务状态、目标进度、失败次数与追踪项；V2.2 格式 12 自动迁移为空任务状态

## V2.2.0 新增

- 每位稳定 NPC 拥有 `-100..100` 好感度与敌对、戒备、中立、友善、信赖五级态度
- 六类职业各有偏好与厌恶礼物；每位 NPC 每个游戏日最多收礼一次，普通礼物也会产生明确反馈
- 村庄声望与个人好感共同影响商店买卖价格，友善与信赖阈值会发放一次性关系奖励
- 敌对 NPC 保留对话入口，但拒绝交易与旅店服务；关系语气会随态度实时变化
- NPC 窗口扩展为商店、收购、赠礼三栏，并显示态度、好感和当前村庄声望
- 存档格式 12 保存 NPC 好感、赠礼日、已领取奖励和村庄声望；V2.1 格式 11 自动迁移为空关系状态

## V2.1.0 新增

- 每座确定性村庄稳定生成村长、杂货商、旅店老板、农夫、守卫和探险家；关键服务 NPC 不会因房屋落点被跳过
- NPC 按完整昼夜进度执行休息、工作、巡逻、经营和社交日程，并沿村庄道路确定性寻路
- 靠近 NPC 按 `E` 打开对话界面；对话按角色、时间阶段和交谈次数轮换
- 杂货商支持双向原子交易：材料换取边境币，再用边境币购买火把、食物和工具；背包不足时整笔撤销
- 旅店老板提供休息到次日清晨，推进昼夜、恢复生命与体力并立即进入自动存档流程
- 离开地表后 NPC 节点完全休眠，返回村庄时由世界种子和保存的交互进度重建
- 存档格式 11 保存 NPC 交谈、交易次数及货币统计；V2.0 格式 10 自动迁移为空 NPC 状态

## V2.0.0 新增

- 群系从 9 类扩展至 12 类，新增针叶林、稀树草原和花甸，并同步扩展生态资源权重
- 普通敌人新增荒野野猪与霜灵；通用随机生成器严格排除精英和 Boss 定义
- 三只世界种子确定的区域首领：林冠巨像、沙海巨兽和霜穹翼龙，分别拥有稳定位置与专属印记奖励
- 按 `M` 打开全屏探索地图；移动会揭开 3×3 区块迷雾，并自动发现村庄、建筑、遗迹、洞穴、地牢和区域首领
- 已发现地点、完成标记、最多 32 个自定义标记及安全营地都通过独立地图状态管理
- 村庄、遗迹、地牢与安全营地可成为快速旅行点；旅行会安全重置当前流送任务和敌人会话
- 存档格式 10 保存迷雾、地点、自定义标记和区域首领完成状态；格式 2–9 均有显式迁移路径
- 世界生成格式升级至 5；生成格式 4 的 V1.7 世界会保留永久差异并迁移到当前生成规则
- 中文字体子集从全部运行时脚本与数据重建，并新增 V2.0 地图字形自动验证

## V1.7.0 新增

- 地表建筑中的稳定地牢入口现在可进入独立的第三世界层，并精确返回原入口
- 每座地牢确定性生成 6 个不重叠房间与完整连通走廊，入口到最终 Boss 房路径可达
- 2 扇锁门、2 把钥匙、4 个一次性陷阱、2 个确定性宝箱，以及专用墙体/锁门碰撞
- 2 名地牢精英与 1 名最终守卫 Boss，只在当前地牢生成并使用稳定出生 ID
- 新增地牢挑战 HUD、固定黑暗表现和地牢专属奖励“守卫遗物”
- 未完成退出会清空本次钥匙、门、陷阱、宝箱和敌人进度；完成后所有已解决状态永久保留
- 墓碑按具体地牢 ID 隔离，避免不同地牢的相同局部坐标互相泄漏
- 存档格式 9 保存当前地牢、锚点、返回位置、尝试次数及完整进度；格式 8 自动迁移为空地牢状态
- 世界生成格式继续保持 4，既有地表校验值不变

## V1.6.0 新增

- 每个确定性入口区域生成一处地表洞穴入口，靠近按 `E` 在地表与地下层之间往返
- 32×32 地下区块使用带全局坐标边界的元胞自动机生成，并修复孤立区域、保留跨区块通道
- 煤、铜、铁三类地下矿脉，可用镐子采集为稳定物品
- 洞穴蝙蝠只在地下层生成，地表敌人候选不再混入洞穴敌人
- 地下宝箱提供确定性战利品，背包不足时保持未开启且不会吞掉或复制物品
- 洞穴墙体碰撞、固定火把和手持火把照明，以及地表天气在地下自动隐藏
- 存档格式 8 分层保存地表/地下采集差异、已开启宝箱、玩家位置与当前世界层
- 格式 2–7 旧存档显式迁移到地表层，原有生成格式 4 和地表校验值保持不变

## V1.5.0 新增

- 确定性村庄中心、中心广场与环形房屋布局
- 商店、火堆、水井、村庄 NPC 和房屋入口语义标记
- 房屋入口到广场的村内道路骨架
- 相邻村庄间的跨区块区域道路
- 路径会比较海拔、水域和基础地形代价，选择成本更低的正交路线
- 道路穿越河流或湖泊时自动生成桥面
- 独立批量村庄渲染层，并清理道路与建筑覆盖位置的原始资源交互

## V1.4.0 新增

- 数据驱动结构资源格式，内置小屋、营地、废墟、神殿和地牢入口五类模板
- 宝箱、敌人出生点和地牢入口语义标记
- 结构以 90° 步进旋转并可水平镜像，变换后保持所有单元与标记
- 基于世界种子和 192×192 格区域的跨区块确定性规划
- 独立共享图集结构层、墙体物理碰撞，以及建筑覆盖资源的显示和交互避让
- `tools/structure_editor.py` 离线模板校验、旋转、镜像与文本预览工具

## V1.3.0 新增

- 基于世界种子和全局坐标的连续河流场，跨区块边界不依赖生成顺序
- 湖泊、低温冰湖和沙漠绿洲，以及河岸过渡带
- 稳定分布的短桥，桥面保持陆地移动速度
- 河流与绿洲涉水减速、湖泊基础游泳、冰面轻度减速和游泳耐力消耗
- 水系作为兼容覆盖层加入，存档格式 7、世界生成格式 4 和 V1.2 地形校验值保持不变

## V1.2.0 新增

- 晴天、降雨、降雪和沙尘四类数据驱动天气
- 按世界种子、天气区域、时间段和群系确定性选择天气
- 8 秒平滑天气过渡，以及雨、雪、沙尘屏幕粒子与程序化环境音
- 降雨等天气会调整资源产量和敌人数量上限
- 格式 7 精确保存当前/目标天气、区域、时段、持续时间和过渡进度
- 格式 2–6 旧存档显式迁移，世界生成格式继续保持 4

## V1.1.0 内容

- 数据驱动的 20 分钟完整昼夜：黎明 1 分钟、白天 12 分钟、黄昏 2 分钟、夜晚 5 分钟
- 阶段末平滑环境颜色过渡，以及 HUD 阶段和进度显示
- 快捷栏选择火把后跟随玩家的柔和照明范围
- 夜间敌人筛选与 1.5 倍数据驱动数量上限
- 仅在夜间显现和可采集的月光花，以及稳定物品“月光花瓣”
- 精确时间保存；退出期间世界时间保持暂停
- 群系规则改为线程安全的标量模型，消除并发生成中的共享 Dictionary 原生崩溃

## V1.0.0 基础内容

- Godot 4.7.1 工程与标准目录结构
- 中文像素风主菜单与可向任意方向探索的程序化世界
- 场景生命周期管理器与全局事件总线
- 可持久化设置、日志轮换和运行时调试面板
- 八方向行走、奔跑、翻滚、耐力消耗与生命接口
- 基于 `CharacterBody2D` 的物理碰撞与平滑边界镜头
- 生命、体力、状态和世界坐标 HUD
- 稳定的 64 位文字/数字世界种子和系统派生种子
- 32×32 区块坐标、负坐标、区块内坐标和稳定区块键
- FastNoiseLite 多尺度海拔场与深水、浅水、沙滩、陆地四级地形
- 大陆、海拔、侵蚀、温度、湿度和细节六个独立噪声域
- 平原、森林、沙漠、雪原、沼泽、山地、海岸和两级海洋共九类群系
- 可直接编辑 `data/biomes.json` 的群系阈值、优先级、颜色和过渡带
- 全局邻域群系清理与气候阈值过渡，避免大量单格碎片
- 活动半径 2、预载半径 3、缓存半径 4 的有界区块生命周期
- 距离/移动方向优先队列和最多 4 个并发 WorkerThreadPool 纯数据任务
- 主线程 `TileMapLayer` 激活/卸载、共享像素图集、内存峰值和流送 HUD
- 地形、群系、气候、海拔四种调试视图与可切换区块边界
- 当前群系、温度、湿度、海拔和侵蚀实时 HUD
- 树木、石头、草丛、花和浆果丛五类稳定资源 ID
- 可直接编辑 `data/resources.json` 的群系密度、间距、耐久、工具和掉落规则
- 按全局候选网格生成且跨区块成立的最小资源间距
- 树木、石头和浆果丛的共享 `TileMapLayer` 物理碰撞
- 从快捷栏装备徒手、木/石斧和木/石镐，按工具类型与强度加速采集
- 确定性物品掉落、32 槽对象池、同类溢出合并和近距离自动拾取
- 独立 `data/items.json` 物品资源、稳定唯一 ID、材料/食物分类和逐物品堆叠上限
- 24 格背包、前 8 格快捷栏、拖拽交换/合并、右键拆半、整组丢弃和分类整理
- 背包满时未拾取数量继续留在地面，拒绝路径不会复制或吞掉物品
- 版本化格子/堆叠存档、完整顺序校验和，以及 V0.7 数量字典到 V0.8 格子的迁移
- 可直接编辑 `data/recipes.json` 的徒手、工作台和火堆配方目录
- 木斧/镐/剑、石斧/镐/剑、工作台、火堆、火把与熟浆果共十个配方
- 材料发现解锁、工作站条件、材料不足提示和背包空间预检
- 克隆背包后提交的原子制作事务，失败不会扣料，成功精确扣料并加入产物
- 独立耐久工具实例、快捷栏耐久显示、逐次采集消耗、破损移除与掉落保持
- 制作发现和工具耐久随存档恢复；V0.7/V0.8 存档可显式迁移到格式 4
- 可直接编辑 `data/weapons.json` 的徒手、木剑和石剑伤害/攻速/范围/击退数据
- 普通攻击、三段连击、攻击冷却、体力消耗和面向八方向的短时 `Area2D` 碰撞框
- 每个攻击 ID 对同一目标最多命中一次，命中后按防御系数计算至少 1 点伤害
- 木剑/石剑有效命中消耗一次耐久，目标承受方向性击退和受击反馈
- 玩家受伤、受击无敌、翻滚无敌、死亡计数、安全位置重生与重生保护
- 死亡时将完整背包存入当前世界层的可持久化墓碑，靠近按 `E` 原样取回物品与耐久
- 武器/连击/冷却/墓碑状态 HUD，以及完整的玩家与敌人双向战斗链路
- 战斗状态和墓碑自格式 5 起持久化，并随当前格式 15 恢复；格式 2–14 旧存档均可显式迁移
- 可直接编辑 `data/enemies.json` 的史莱姆、野狼和洞穴蝙蝠属性、群系、行为与掉落规则
- `IDLE/WANDER/ALERT/CHASE/ATTACK/HURT/RETURN/DEAD` 八状态通用敌人状态机
- 史莱姆跳动、野狼快速扑击和蝙蝠振翅三类像素表现与差异化速度/攻击节奏
- 基于世界种子、带符号区块坐标和群系的确定性敌人候选；敌人不在水中或屏幕内突然出现
- 全局最多 18 只、每区块最多 3 个候选、远距离逻辑休眠、超距卸载和 49 区块候选缓存裁剪
- 敌人 `CharacterBody2D` 物理碰撞、碰墙切向绕障、警觉追踪、活动区返回和冷却攻击
- 敌人死亡后通过既有 32 槽对象池掉落史莱姆凝胶、狼皮与蝙蝠翼并自动拾取
- 敌人数量、休眠、种类和活跃状态 HUD；运行时敌人不额外改变格式 6 存档或格式 4 世界字节
- 300 秒基础昼夜循环、持续天数和存档时间恢复；夜晚以独立画布遮罩呈现
- 可直接编辑 `data/milestones.json` 的遗迹搜索、Boss 属性和一次性奖励规则
- 世界种子唯一确定的近出生点古老遗迹、方向/距离线索与发现状态
- 180 生命的小型遗迹守卫、追击/震地攻击、受击、死亡和封印解除流程
- 击败 Boss 后按 `E` 领取一次性远古核心；背包满时奖励留在遗迹且不会复制
- 遗迹发现、Boss 击败和奖励领取随格式 6 存档恢复；格式 2–5 均可显式迁移
- 程序化 PCM 基础攻击、阻挡、成功、Boss 与里程碑音效，不依赖外部服务
- 昼夜、当前目标、遗迹线索和 Boss 状态 HUD，打通首个完整生存闭环
- 会话内资源差异与持久背包；返回已探索区块时不会恢复已采集资源
- 世界创建界面、1–32 字世界名称和文字/数字种子
- `world.json` 世界元数据与 `player.json` 位置、生命、体力、工具和采集计数
- 仅在永久变化后创建的 `chunks/surface/<x>_<y>.json` 区块差异
- 30 秒后台自动保存、`Ctrl+S` 手动保存、返回菜单/正常退出保存
- 临时文件事务提交、最多五份时间戳备份和退出前任务汇合
- 文件、行号和原因明确的损坏/版本不兼容提示
- “继续游戏”载入最近的有效世界，正确恢复玩家与被采集资源
- 自动化结构检查、生成顺序/接缝、后台线程及 30 分钟缓存策略模拟
- Windows/Linux 导出预设、架构文档和完整迭代路线

## 运行

1. 安装或解压 Godot 4.7.1 stable。
2. 在 Godot Project Manager 中导入根目录的 `project.godot`。
3. 点击运行，或使用命令：

```bash
godot4 --path .
```

## 测试

Windows PowerShell：

```powershell
.\tools\run_tests.ps1 -GodotBin C:\path\to\godot.exe -PythonBin C:\path\to\python.exe
.\tools\build_release.ps1 -GodotBin C:\path\to\godot.exe -PythonBin C:\path\to\python.exe
```

Linux/macOS shell：

```bash
GODOT_BIN=/path/to/godot tools/run_tests.sh
```

测试流程依次执行结构验证、资源导入、自动测试、主菜单冒烟测试和游戏场景冒烟测试；日志中出现脚本错误也会直接阻断发布。

V1.0.1 使用锚点与安全区适配 1280×720、1920×1080、2560×1440 和常见超宽屏；世界画面以 `expand` 拉伸模式铺满窗口，像素纹理保持 nearest 过滤。由于 1920×1080 相对 1280×720 是 1.5 倍，窗口缩放采用 fractional，避免 integer 模式留下未铺满区域。

生成并验证 Windows/Linux 发布包：

```bash
GODOT_BIN=/path/to/godot tools/build_release.sh
```

游戏中按 `J` 或鼠标左键攻击，按 `E` 采集和交互，按 `Q` 循环工具，按 `G` 使用选中的食物或药水，按 `1–8` 选择快捷栏；按 `K` 建造、`H` 农业、`U` 养殖、`T` 加工、`C` 制作、`I`/`Tab` 背包、`M` 地图、`L` 任务、`F` 阵营、`V` 事件、`P` 区域进度。背包内按 `R` 整理，按 `Ctrl+S` 手动保存；按 `N` 循环地形调试视图，按 `B` 切换区块边界。

## 项目结构

```text
assets/       品牌、图形、音频和字体资源
data/         数据驱动的物品、配方、群系和内容
docs/         架构、生成、存档、测试与开发记录
scenes/       Godot 场景
scripts/      按核心、世界、生成和玩法领域组织的脚本
tests/        自动测试与验证场景
tools/        构建、验证和开发工具
releases/     每版测试、审查和问题记录
```

## 路线

V1.0.0 只是第一个完整可玩节点，项目不会在此停止。完整路线见 [`docs/release-roadmap.md`](docs/release-roadmap.md)，最终目标是包含完整世界、文明、生存、建造、海洋、季节、主线和终局的 V5.0.0。

存档目录、JSON 字段、差异条件、事务和备份规则见 [`docs/save-format.md`](docs/save-format.md)。

## License

Code is released under the MIT License. Third-party assets, if introduced later, will be listed with their individual licenses.

The bundled Noto Sans CJK SC project subset is licensed under the SIL Open Font License 1.1. The subset is expanded as new in-game text is introduced. See [`assets/fonts/OFL-1.1.txt`](assets/fonts/OFL-1.1.txt).
